package com.ths.thethskitchen_git_ver2021072601

import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.FrameLayout
import android.widget.Toast
import com.ths.thethskitchen_git_ver2021072601.databinding.ActivitySearchBinding

class SearchActivity {
//    val binding by lazy { ActivitySearchBinding.inflate(layoutInflater)}
//    override fun onCreate(savedInstanceState: Bundle?) {
//        super.onCreate(savedInstanceState)
//        setContentView(R.layout.activity_search)
//        binding.btnSearch.setOnClickListener{
//            if (binding.editSearch.text.toString() != ""){
//                Intent(this, ListActivity::class.java).apply {
//                    putExtra("search", binding.editSearch.text.toString())
//                    addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
//                }.run {applicationContext.startActivity(this) }
//            } else {
//                Toast.makeText(this,"검색어를 입력하세요", Toast.LENGTH_SHORT).show()
//            }
//        }
//    }
}